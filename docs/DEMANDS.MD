# 当前需求清单

日期：2026-03-24
版本目标：v2.0.0

## 本轮确认需求

1. 将 `selected_wsi.csv` 与 `failed_wsi.csv` 中的 `path` 字段，从当前设备上的绝对路径改为基于 `--input_dir` 的相对路径。
2. 保持当前 `--seed` 为命令行参数，不新增 CSV 中的 `seed` 列。
3. 为兼容旧历史结果，程序启动时若检测到 `selected_wsi.csv` 中仍为绝对路径，应自动原地迁移为相对路径。
4. 若旧历史 CSV 中存在无法映射到当前 `--input_dir` 下的绝对路径，程序应显式报错，不允许静默跳过。
5. 在相同数据集目录结构、相同轮次、相同 `--seed` 的前提下，不同设备应选择到相同的 slide。
6. 实现时遵循最小必要改动，不重构与当前需求无关的主流程。
7. 取消 `--version` 命令行参数，并将本次不兼容 CLI 变更按主版本升级处理。

## 交付要求

1. 更新主脚本逻辑以支持相对路径输出与旧历史迁移。
2. 补充或更新自动化测试，覆盖相对路径输出、旧历史迁移成功、旧历史迁移失败边界。
3. 更新 `README.md` 中关于 CSV 字段和增量轮次行为的说明。
4. 维护 `docs/CHANGELOG.md`，记录本次版本变更、影响文件和验证结果。
